This exe file scans for all uncorporated and unauthorized exe files or scripts that may be affiliated/used on roblox. After the scan is finished/file is ended, it will create a text document on your desktop or main downloads folder named rbxscanresults. Here it will show all suspiciously found files. It will NOT delete nor quarantine any files, but will only alert of them (and the path to where you can find them).
